# 🔍 RAGLOX v3.0 - دليل الفحص الشامل للمسؤول

## 📋 معلومات الوصول

### روابط الخدمات
| الخدمة | الرابط | الحالة |
|--------|--------|--------|
| **Frontend** | http://172.245.232.188:3000 | ✅ يعمل |
| **Backend API** | http://172.245.232.188:8000 | ✅ يعمل |
| **Swagger Docs** | http://172.245.232.188:8000/docs | ✅ يعمل |
| **ReDoc** | http://172.245.232.188:8000/redoc | ✅ يعمل |
| **Health Check** | http://172.245.232.188:8000/health | ✅ يعمل |

---

## 🎯 المرحلة 1: فحص الواجهة الأمامية (Frontend)

### 1.1 الصفحة الرئيسية
- [ ] تحقق من تحميل الصفحة الرئيسية بنجاح
- [ ] تحقق من عرض شعار RAGLOX
- [ ] تحقق من استجابة التصميم (Responsive)
- [ ] تحقق من عدم وجود أخطاء في Console

### 1.2 نظام المصادقة (Authentication)
```
📍 المسار: /login و /register
```
- [ ] **تسجيل مستخدم جديد:**
  - الاسم: `test_user`
  - البريد: `test@raglox.io`
  - كلمة المرور: `Test@123456`
- [ ] التحقق من رسالة النجاح
- [ ] **تسجيل الدخول:**
  - استخدام البيانات المسجلة
- [ ] التحقق من الانتقال إلى Dashboard
- [ ] التحقق من حفظ Token في localStorage

### 1.3 لوحة التحكم (Dashboard)
- [ ] عرض إحصائيات المهام
- [ ] عرض المهام النشطة
- [ ] عرض الأهداف المكتشفة
- [ ] عرض الثغرات

### 1.4 إنشاء مهمة جديدة (Mission)
```
📍 المسار: /missions/create
```
- [ ] ملء نموذج إنشاء مهمة:
  - **الاسم:** Test Mission
  - **الوصف:** Penetration test for QA
  - **النطاق:** 192.168.1.0/24
  - **الأهداف:** Domain access
- [ ] النقر على "إنشاء المهمة"
- [ ] التحقق من الانتقال إلى صفحة المهمة

### 1.5 HITL Chat Interface
```
📍 المسار: /missions/{id}/chat
```
- [ ] إرسال رسالة للوكيل
- [ ] استقبال الرد
- [ ] فحص WebSocket connection
- [ ] اختبار إرسال أمر موافقة

### 1.6 صفحة الفوترة (Billing)
```
📍 المسار: /billing
```
- [ ] عرض الخطط المتاحة:
  - Free: $0
  - Starter: $49/شهر
  - Professional: $199/شهر
  - Enterprise: $499/شهر
- [ ] التحقق من زر "اشتراك"

---

## 🔌 المرحلة 2: فحص API Backend

### 2.1 نقاط النهاية الأساسية

#### Health Check
```bash
curl http://172.245.232.188:8000/health
```
**النتيجة المتوقعة:**
```json
{
    "status": "healthy",
    "components": {
        "api": "healthy",
        "blackboard": "healthy",
        "knowledge": "loaded"
    }
}
```

#### Root Endpoint
```bash
curl http://172.245.232.188:8000/
```
**النتيجة المتوقعة:**
```json
{
    "name": "RAGLOX",
    "version": "3.0.0",
    "architecture": "Blackboard",
    "status": "operational"
}
```

### 2.2 المصادقة (Authentication)

#### تسجيل مستخدم
```bash
curl -X POST http://172.245.232.188:8000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "qa_test@raglox.io",
    "password": "QaTest@2024",
    "full_name": "QA Tester"
  }'
```

#### تسجيل الدخول
```bash
curl -X POST http://172.245.232.188:8000/api/v1/auth/login \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=qa_test@raglox.io&password=QaTest@2024"
```

### 2.3 الفوترة (Billing)

#### قائمة الخطط
```bash
curl http://172.245.232.188:8000/api/v1/billing/plans
```
**يجب أن يُرجع 4 خطط**

### 2.4 قاعدة المعرفة (Knowledge)

#### إحصائيات
```bash
curl http://172.245.232.188:8000/api/v1/knowledge/stats
```
**التحقق من:**
- `total_rx_modules`: 1761
- `total_nuclei_templates`: 11927
- `total_techniques`: 327
- `total_tactics`: 14

### 2.5 الاستغلال (Exploitation)

#### حالة الاستغلال
```bash
curl http://172.245.232.188:8000/api/v1/exploitation/status/exploitation
```
**النتيجة المتوقعة:**
```json
{
    "mode": "simulation",
    "c2": {
        "enabled": true,
        "active_sessions": <number>
    },
    "capabilities": {
        "post_exploitation": true,
        "network_pivoting": true,
        "credential_harvesting": true
    }
}
```

---

## 🛡️ المرحلة 3: فحص الأمان

### 3.1 Rate Limiting
```bash
# إرسال 20 طلب متتالي
for i in {1..20}; do 
  curl -s -o /dev/null -w "%{http_code}\n" http://172.245.232.188:8000/health
done
```
**يجب أن يُرجع 429 بعد تجاوز الحد**

### 3.2 Input Validation
```bash
# اختبار XSS
curl -X POST http://172.245.232.188:8000/api/v1/missions \
  -H "Content-Type: application/json" \
  -d '{"name": "<script>alert(1)</script>"}'
```
**يجب أن يُرفض الإدخال**

### 3.3 JWT Security
- [ ] التحقق من انتهاء صلاحية Token بعد 24 ساعة
- [ ] التحقق من رفض Tokens غير صالحة

---

## 🔄 المرحلة 4: فحص WebSocket

### 4.1 اتصال WebSocket
```javascript
// في Console المتصفح
const ws = new WebSocket('ws://172.245.232.188:8000/api/v1/ws/{mission_id}');
ws.onopen = () => console.log('Connected');
ws.onmessage = (e) => console.log('Message:', e.data);
ws.send(JSON.stringify({type: 'ping'}));
```

### 4.2 التحقق من:
- [ ] الاتصال الناجح
- [ ] استقبال heartbeat
- [ ] إعادة الاتصال التلقائي

---

## 📊 المرحلة 5: فحص الأداء

### 5.1 وقت الاستجابة
```bash
time curl -s http://172.245.232.188:8000/health
```
**المتوقع:** < 500ms

### 5.2 اختبار التحمل
```bash
# استخدم ab أو wrk
ab -n 100 -c 10 http://172.245.232.188:8000/health
```

---

## 📝 نموذج تقرير المشاكل

### عند اكتشاف مشكلة، سجّل:

```markdown
## 🐛 تقرير مشكلة

**المكون:** [Frontend/Backend/WebSocket/Database]
**الخطورة:** [عالية/متوسطة/منخفضة]
**المسار/Endpoint:** 
**خطوات إعادة الإنتاج:**
1. 
2. 
3. 

**النتيجة المتوقعة:**

**النتيجة الفعلية:**

**لقطة شاشة/سجل الخطأ:**

**معلومات إضافية:**
```

---

## ✅ قائمة التحقق النهائية

### الخدمات
- [x] PostgreSQL متصل ويعمل
- [x] Redis/Blackboard يعمل
- [x] Knowledge Base محمّل (1,761 وحدة)
- [x] LLM Service (BlackBox AI) يعمل
- [x] Billing Service (Stripe) مهيأ
- [x] SSH Connection Manager جاهز
- [x] C2 Session Manager يعمل
- [x] Environment Manager جاهز
- [x] Workflow Orchestrator جاهز

### الواجهة
- [ ] تسجيل الدخول يعمل
- [ ] Dashboard يعرض البيانات
- [ ] إنشاء المهام يعمل
- [ ] HITL Chat يعمل
- [ ] صفحة الفوترة تعمل

### الأمان
- [ ] Rate Limiting نشط
- [ ] Input Validation نشط
- [ ] JWT يعمل بشكل صحيح
- [ ] CORS مهيأ

### الأداء
- [ ] وقت الاستجابة مقبول
- [ ] لا توجد أخطاء في السجلات

---

## 📞 معلومات التواصل

**للإبلاغ عن مشاكل حرجة:**
- السجلات: `tail -f /tmp/backend.log`
- Frontend logs: Console في المتصفح

**الموارد:**
- API Documentation: http://172.245.232.188:8000/docs
- ReDoc: http://172.245.232.188:8000/redoc

---

## 🏁 الإصدار والتاريخ

- **الإصدار:** RAGLOX v3.0.0
- **تاريخ الفحص:** 2026-01-06
- **البيئة:** Production Sandbox

---

## ملاحظات هامة

1. **وضع المحاكاة:** النظام يعمل في وضع SIMULATION للاستغلال لأن Metasploit RPC غير متاح. هذا طبيعي في بيئة الاختبار.

2. **SSH جاهز:** يمكن الاتصال بأي خادم SSH حقيقي من خلال Environment Manager.

3. **C2 Sessions:** يوجد جلسات C2 نشطة في النظام.

4. **LLM:** BlackBox AI متصل ويعمل للتحليل الذكي.
